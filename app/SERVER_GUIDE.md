# 🚀 วิธีติดตั้ง Pastel Finance ผ่าน Local Server

## วิธีที่ 1: ใช้ Python (ง่ายที่สุด)

### Windows:
```bash
cd "C:\Users\aum28\OneDrive\Desktop\app"
python -m http.server 8000
```

### macOS/Linux:
```bash
cd ~/Desktop/app
python -m http.server 8000
```

จากนั้นเปิด `http://localhost:8000` ในเบราว์เซอร์

---

## วิธีที่ 2: ใช้ Node.js

### ขั้น 1: ติดตั้ง Node.js
- ดาวน์โหลดจาก https://nodejs.org/
- ติดตั้ง (เลือก LTS version)
- ตรวจสอบ: เปิด Command Prompt และพิมพ์ `node --version`

### ขั้น 2: รันเซิร์ฟเวอร์
```bash
cd "C:\Users\aum28\OneDrive\Desktop\app"
node server.js
```

### ขั้น 3: เปิดในเบราว์เซอร์
- PC: `http://localhost:3000`
- Android: `http://<PC-IP>:3000`

---

## วิธีที่ 3: ใช้ Visual Studio Code Live Server

### ขั้น 1: ติดตั้ง Extension
1. เปิด VS Code
2. ไปที่ Extensions (Ctrl+Shift+X)
3. ค้นหา "Live Server"
4. ติดตั้ง (ผู้สร้าง: Ritwick Dey)

### ขั้น 2: รัน
1. คลิกขวาที่ `index.html`
2. เลือก "Open with Live Server"
3. เบราว์เซอร์จะเปิด `http://localhost:5500`

---

## วิธีที่ 4: ใช้ npm http-server

### ขั้น 1: ติดตั้ง npm packages
```bash
npm install -g http-server
```

### ขั้น 2: รันเซิร์ฟเวอร์
```bash
cd "C:\Users\aum28\OneDrive\Desktop\app"
http-server -p 8080
```

### ขั้น 3: เปิด
- PC: `http://localhost:8080`
- Android: `http://<PC-IP>:8080`

---

## 🔍 หาที่อยู่ IP ของ PC

### Windows:
```bash
ipconfig
```
หาบรรทัดที่ว่า "IPv4 Address" (ซึ่งมักจะเป็น 192.168.x.x หรือ 10.x.x.x)

### macOS/Linux:
```bash
ifconfig
```
หา "inet" (ระวัง localhost = 127.0.0.1)

---

## 📱 เชื่อมต่อจาก Android

1. **ตรวจสอบเครือข่าย**: Android ต้องเชื่อมต่อกับ WiFi เดียวกับ PC
2. **เปิด Chrome บน Android**
3. **พิมพ์ URL**: `http://<PC-IP>:PORT`
   - ตัวอย่าง: `http://192.168.1.100:8000`
4. **รอให้หน้าโหลด**
5. **ตั้งแต่ Chrome รุ่น 80+** จะมีปุ่มติดตั้ง

---

## ⚠️ ปัญหา

### Android ไม่สามารถเชื่อมต่อได้
- ✅ ตรวจสอบว่า PC กับ Android อยู่ WiFi เดียวกัน
- ✅ ปิด Firewall ชั่วคราว (Windows)
- ✅ ใช้ Port ที่ต่างออกไป (8000, 8080, 3000)

### หน้ากำลังติดตั้งสลับไป
- ✅ บอก "บนสำหรับหลังจากการทำขั้นแรกภาพเต็มหน้า"
- ✅ คลิก "ติดตั้ง"
- ✅ รอให้สำเร็จ

### ไม่มีข้อมูลหลังติดตั้ง
- ✅ ข้อมูลจาก PC Browser ≠ ข้อมูลจาก APP ที่ติดตั้ง
- ✅ เพิ่มข้อมูลใหม่ในแอป

---

## 💡 คำแนะนำ

### สำหรับการทดสอบ:
```bash
# ใช้ Python ตรงเน้นง่ายสุด
python -m http.server 8000
```

### สำหรับการผลิต:
```bash
# ใช้ Node.js http-server หรือ Express
node server.js
```

### สำหรับการพัฒนา:
```bash
# ใช้ Live Server ใน VS Code
# อัตโนมัติรีโหลดเมื่อบันทึกไฟล์
```

---

## 🔒 HTTPS (ปลอดภัยกว่า)

PWA ทำงานดีกับ HTTPS ที่มี SSL Certificate:

```bash
# สร้าง self-signed certificate (สำหรับทดสอบเท่านั้น)
openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365
```

---

## ✅ ตรวจสอบ

หลังจากเปิดตัวแล้ว:

1. ✅ เปิด DevTools (F12)
2. ✅ ไปที่ Application tab
3. ✅ ตรวจสอบ Service Worker (ควรเป็นสีเขียว "activated")
4. ✅ ตรวจสอบ Manifest (ควรโหลดได้)
5. ✅ ตรวจสอบ Cache (ควรมีไฟล์)

---

## 🎉 สำเร็จ!

หากทุกอย่างเรียบร้อย คุณควรสามารถ:
- ✅ ใช้งาน Pastel Finance ใน PC
- ✅ เปิดจาก Android โทรศัพท์
- ✅ ติดตั้งเป็นแอป
- ✅ ใช้ออฟไลน์

**ขอให้สนุกกับการใช้! 💰**
